class Vehicul {
    string marca;
    int an;

    Vehicul(string m) {
        marca = m;
        an = 2024;
    }

    string getMarca() {
        return marca;
    }
}

Vehicul v;

Vehicul creeazaMasina(string nume) {
    v = new Vehicul(nume);
    return v;
}

int x;
string salut;
Vehicul masinaMea;
string status;
int calcul;

begin_progr
    salut = "Masina noua";
    x = 10;
    
    masinaMea = creeazaMasina("Dacia");

    calcul = x * 2 + 50 / 2;

    if (calcul > 20) {
        status = "Calcul finalizat";
        print(status);
    }

    while (x > 0) {
        x = x - 1;
    }

    print(salut);
    print(x);
end_progr